- allows two incompatible interfaces to work together by providing a wrapper that translates one interface into another

- acts as a bridge between different systems or components

- adapter class implements new interface and wraps new one but use inside the old

- composition by dependency inversion --> aggregation